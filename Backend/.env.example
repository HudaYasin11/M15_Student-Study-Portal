# Copy this file to ".env" (same folder) and replace YOUR_PASSWORD with the
# password you set during PostgreSQL install. Never commit the real .env to GitHub.

DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/m15_study_portal

PORT=5000
